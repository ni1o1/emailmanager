services:
  emailmanager:
    build: .
    container_name: emailmanager
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./state.db:/app/state.db
      - ./logs:/app/logs
